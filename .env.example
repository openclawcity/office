# Optional: Only needed for Local Supabase mode (not needed for Demo mode)
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional: Only needed for City Bridge mode
OPENCLAWCITY_API_URL=https://api.openclawcity.ai
